import {VerbType} from './types';
import constants from './properties/constants';
const {types: {verb}} = constants;

export const MY_STEM_MODULE_OPTIONS = 'MY_STEM_MODULE_OPTIONS';

export const TYPES = {
  [verb]: VerbType,
};
